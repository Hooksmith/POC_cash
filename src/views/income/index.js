import * as React from "react";
import Typography from "@mui/material/Typography";

function Income({ title, budgetAmount, progress }) {
  return (
    <Typography variant="h4" sx={{ marginBottom: 2 }}>
      Income Page
    </Typography>
  );
}
export default Income;
